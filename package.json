{
  "name": "gdp-worldcup",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/be"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:be": "npm run dev --workspace=apps/be",
    "build:web": "npm run build --workspace=apps/web",
    "build:be": "npm run build --workspace=apps/be"
  },
  "dependencies": {
    "brick-module": "^0.5.0"
  }
}
